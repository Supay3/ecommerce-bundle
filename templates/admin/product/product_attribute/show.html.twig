{% extends 'admin/admin_base.html.twig' %}

{% block title %}ProductAttribute{% endblock %}

{% block body %}
    <div class="container">
        <h1>ProductAttribute</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_attribute.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_attribute.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ product_attribute.type }}</td>
            </tr>
            <tr>
                <th>StorageType</th>
                <td>{{ product_attribute.storageType }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ product_attribute.createdAt ? product_attribute.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ product_attribute.updatedAt ? product_attribute.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path(constant('App\\Controller\\RouteName::ADMIN_PRODUCT_ATTRIBUTE_INDEX')) }}">back to list</a>

        <a href="{{ path(constant('App\\Controller\\RouteName::ADMIN_PRODUCT_ATTRIBUTE_EDIT'), {'id': product_attribute.id}) }}">edit</a>

        {{ include('admin/product/product_attribute/_delete_form.html.twig') }}
    </div>
{% endblock %}
