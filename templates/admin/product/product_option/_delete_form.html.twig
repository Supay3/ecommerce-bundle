<form method="post" action="{{ path(constant('App\\Controller\\RouteName::ADMIN_PRODUCT_OPTION_DELETE'), {'id': product_option.id}) }}" class="admin-delete-form" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_option.id) }}">
    <button class="admin-delete">Delete</button>
</form>
